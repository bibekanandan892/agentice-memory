"""MCP server exposing memlayer as tools any MCP-aware client (Claude
Desktop, Claude Code, etc.) can call directly.

Stretch Phase 5 of the project plan — the same Memory instance used by the
CLI and the REST server, now callable as MCP tools over stdio.
"""
