"""FastAPI server wrapping a single memlayer.Memory instance.

Stretch Phase 4 of the project plan — an optional HTTP surface over memlayer,
so any non-Python client (or a future web UI) can use the same memory
library the CLI does, without embedding it directly.
"""
